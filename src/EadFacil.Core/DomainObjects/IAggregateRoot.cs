namespace EadFacil.Core.DomainObjects;

public interface IAggregateRoot
{
    // This interface is intentionally left empty.
    // It serves as a marker interface to identify aggregate root entities in the domain.
    // Aggregate roots are the main entry points for interacting with aggregates in Domain-Driven Design (DDD).
}